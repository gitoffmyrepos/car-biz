# Backend Deployment for GigWheels
# FastAPI application serving API endpoints
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gigwheels-backend
  labels:
    app: gigwheels
    component: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gigwheels
      component: backend
  template:
    metadata:
      labels:
        app: gigwheels
        component: backend
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8000"
        prometheus.io/path: "/metrics"
    spec:
      serviceAccountName: gigwheels-sa
      # Pod anti-affinity for high availability
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: component
                  operator: In
                  values:
                  - backend
              topologyKey: kubernetes.io/hostname
      initContainers:
      - name: wait-for-postgres
        image: postgres:16-alpine
        imagePullPolicy: IfNotPresent
        command:
        - sh
        - -c
        - |
          until pg_isready -h "$POSTGRES_HOST" -p "$POSTGRES_PORT" -U "$POSTGRES_USER"; do
            echo "waiting for postgres at $POSTGRES_HOST:$POSTGRES_PORT ..."
            sleep 2
          done
          echo "postgres is ready"
        envFrom:
        - configMapRef:
            name: gigwheels-config
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: database-user
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 250m
            memory: 128Mi
        securityContext:
          runAsNonRoot: true
          runAsUser: 70
          runAsGroup: 70
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
      containers:
      - name: backend
        image: harbor.strategybase.io/sb-custom-docker-images/gigwheels-backend:prod-latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
          protocol: TCP
        envFrom:
        - configMapRef:
            name: gigwheels-config
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        # Secrets from Vault via ExternalSecrets
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: redis-url
        - name: VAULT_TOKEN
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: vault-token
        - name: RESEND_API_KEY
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: resend-api-key
        - name: SMTP_USER
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: smtp-user
        - name: SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: smtp-password
        - name: MINIO_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: minio-access-key
        - name: S3_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: minio-access-key
        - name: MINIO_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: minio-secret-key
        - name: S3_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: minio-secret-key
        - name: OIDC_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: gigwheels-secrets
              key: oidc-client-secret
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 2Gi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
            scheme: HTTP
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 10
          failureThreshold: 3
          successThreshold: 1
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
          successThreshold: 1
        startupProbe:
          httpGet:
            path: /health
            port: 8000
            scheme: HTTP
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 30
        volumeMounts:
        - name: tmp
          mountPath: /tmp
        - name: app-logs
          mountPath: /app/logs
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          runAsGroup: 1000
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
      imagePullSecrets:
      - name: harbor-registry-secret
      securityContext:
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      volumes:
      - name: tmp
        emptyDir:
          sizeLimit: 100Mi
      - name: app-logs
        emptyDir:
          sizeLimit: 500Mi
---
# Backend Service
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: gigwheels
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: gigwheels
    component: backend
